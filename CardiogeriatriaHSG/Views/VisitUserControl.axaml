<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:CardiogeriatriaHSG.ViewModels"
             xmlns:views="clr-namespace:CardiogeriatriaHSG.Views"
             xmlns:viewmodels="clr-namespace:CardiogeriatriaHSG.ViewModels.VisitContent"
             xmlns:visitContent="clr-namespace:CardiogeriatriaHSG.Views.VisitContent"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="CardiogeriatriaHSG.Views.VisitUserControl"
             x:DataType="viewModels:VisitUserControlViewModel">
    
    <Design.DataContext>
        <!-- This only sets the DataContext for the previewer in an IDE,
             to set the actual DataContext for runtime, set the DataContext property in code (look at App.axaml.cs) -->
        <viewModels:VisitUserControlViewModel/>
    </Design.DataContext>
    
    <Grid
        ColumnDefinitions="Auto, *, Auto"
        RowDefinitions="Auto, *, Auto">
        <views:HeaderUserControl Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3"/>
        <views:SidebarUserControl Grid.Row="1" Grid.Column="0" />
        
        <ContentControl Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Content="{Binding CurrentContent}" />
        
    </Grid>
</UserControl>
